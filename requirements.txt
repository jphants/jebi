opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# OCR para deteccion de numero de camion (opcional pero recomendado)
# Si falla la instalacion, el sistema usa clasificacion visual como fallback
easyocr>=1.7.0
